/*#include <iostream>;
   using namespace std;

   void groupDNA(string str, int n)
   {
        int index;
        for (index = 0; index < str.length(); index = index + n) {

                group = "";
                for (int pos=0; pos < n; pos = pos+1)
                {
                        group = group + str[index+pos]
                }

                cout << " " << group;

        }





   }
 */
